# Port Setting (Render uses the PORT variable automatically)
server.port=${PORT:8080}

# Supabase Connection Settings
# Format: ${VARIABLE_FROM_RENDER:FALLBACK_FOR_LOCAL_PC}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.qomudjpxwllyoqytrpvu.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Write this down!}

# Database Driver and Hibernate Settings
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect