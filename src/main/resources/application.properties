# Server Port
server.port=${PORT:8080}

# Database URL
# Fallback is set to the Transaction Pooler (Port 6543) for better compatibility
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-eu-central-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}

# Username
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.qomudjpxwllyoqytrpvu}

# Password
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Rohannaik@06}

# Common Settings
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update

# Essential for Render: Force PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true