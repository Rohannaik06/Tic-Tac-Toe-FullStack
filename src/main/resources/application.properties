# Port Setting (Render uses the PORT variable automatically)
server.port=${PORT:8080}

# Supabase Connection Settings
# We use the JDBC format provided in your Supabase settings
spring.datasource.url=jdbc:postgresql://db.qomudjpxwllyoqytrpvu.supabase.co:5432/postgres?tcpKeepAlive=true&preferIPv4Stack=true

spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}

# IMPORTANT: Change 'YOUR_REAL_PASSWORD' to your actual Supabase password
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Rohannaik@06}

# Database Driver and Hibernate Settings
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect